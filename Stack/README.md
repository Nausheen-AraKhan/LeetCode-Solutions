# 📚 Stack – LeetCode Solutions

This folder contains my solutions to **Stack** problems from LeetCode, implemented in **C++**.

## 📖 About Stack

A **Stack** is a linear data structure that follows the **LIFO (Last In, First Out)** principle.

Operations are performed only at one end, called the **top**.

### Basic Operations
- 🔹 `push()` – Insert an element onto the stack.
- 🔹 `pop()` – Remove the top element.
- 🔹 `top()` – Access the top element.
- 🔹 `empty()` – Check whether the stack is empty.
- 🔹 `size()` – Return the number of elements.

---

⭐️PROBLEMS
* Prob 394: Decode String | MEDIUM
* Prob 20: Valid Parantheses | EASY
